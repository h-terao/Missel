# @package _global_

defaults:
  - override /learner: uda.yaml
  - override /model: wrn28_2.yaml
  - override /optimizer: sgd.yaml
  - override /dataset: cifar10.yaml
  - override /callbacks: default.yaml

dataset:
  uratio: 7
  num_labels: ???